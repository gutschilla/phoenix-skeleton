<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Administrationsinterface</title>
        <meta name="description" content="Administrationsinterface" />
        {% block head %}
            <link rel="stylesheet" href="bower_components/bootstrap/dist/css/bootstrap.min.css" />
            <script src="bower_components/jquery/dist/jquery.min.js"          type="text/javascript"></script>
            <script src="bower_components/underscore/underscore-min.js"       type="text/javascript"></script>
            <script src="bower_components/bootstrap/dist/js/bootstrap.min.js" type="text/javascript"></script>
        {% endblock %}
        <style type="text/css">
            nav li.before-dropdown > a { padding-right: 0; }
            nav li.before-dropdown + li.dropdown > a { padding-left: 8px; }
            #all .navbar-default .navbar-nav > .open > a { background-color: #bbb }
        </style>
    </head>
    <body>
        <!--[if lt IE 8]>
            <p class="alert alert-danger">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->
        <div id="all">
            <nav class="navbar navbar-inverse" role="navigation">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="/admin">
                            {% block brand  %}
                                <small>Admin</small> Hello2
                            {% endblock %}
                        </a>
                    </div>

                    <!--
                    :   my $current_node = $c.navigation.current_node;
                    :   macro navigation -> $tree, $level {
                        :   my $daughters = $tree.arrayref_of('allowed_daughters', $c );
                        :   if ( $daughters.size() ) {
                            :   my $classes = $level ? ['dropdown-menu'] : ['nav', 'navbar-nav'];
                            :   my $roles   = $level ? ['none']          : ['menu'];
                            <ul class="<: $classes.join(' ') :>" role="<: $roles.join(' ') :>" >
                                :   for $daughters -> $node {
                                    :   if ( $node.arrayref_of('allowed_daughters', $c ).size() ) {
                                            <li class="before-dropdown <: $current_node == $node ? 'active' : 'inactive' :>"><a href="<: $node.url :>"><: $node.name :></a></li>
                                            <li class="dropdown <: $current_node == $node ? 'active' : 'inactive' :>">
                                                <a href="#" class="dropdown-toggle " data-toggle="dropdown"><b class="caret"></b></a>
                                                :   navigation( $node, $level + 1 )
                                            </li>
                                    :   }
                                    :   else {
                                            <li class="<: $current_node == $node ? 'active' : 'inactive' :>"><a href="<: $node.url :>"><: $node.name :></a></li>
                                            :   navigation( $node, $level + 1 )
                                    :   }
                                :   }
                            </ul>
                        :   }
                    :   }
                    :   navigation( $c.navigation.node_of('/admin', 'navi'), 0 );
                    -->

                    <ul class="nav navbar-nav navbar-right <: 'hidden' if not $c.user :>">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Konto<b class="caret"></b></a>
                            <ul class="dropdown-menu" role="menu">
                                <li><a href="<: $c.uri_for('/auth/do_logout') :>">Logout</a></li>
                            </ul>
                        </li>
                    </ul>
                </div> <!--/.container-fluid-->
            </nav>

            <div id="content">
            {% block content %}{% endblock %}
            </div>
        </div>
    </body>
</html>
